# Vintly 작업 기준

이 문서는 이 저장소에서 작업하는 Codex와 기타 코딩 에이전트가 따라야 할 프로젝트 기준이다.

## 제품 요구사항

이 문서가 제품 요구사항과 저장소 작업 기준의 단일 원본이다. 기능의 목적, 사용자 흐름, 입력 검증 또는 디자인 방향을 바꾸면 코드와 함께 이 문서를 갱신한다. 세부 디자인·기술 선택의 근거는 `docs/decisions`, 미완료 작업은 `docs/todo.md`에 기록한다.

### 프로젝트 개요

- 프로젝트 이름: Vintly
- 한 줄 설명: 빈티지 숍과 제품 정보를 공유하고 소통하는 Flutter 모바일 커뮤니티 앱
- 기획 배경: 빈티지에 관심 있는 사용자가 매장의 위치와 상세 정보를 찾고, 좋아요·댓글·게시글을 통해 정보를 나눌 수 있는 공간을 제공한다.
- 기본 UI와 사용자 메시지는 한국어를 사용한다.

### 타깃 사용자와 사용 시나리오

- 연령대: 20~40대
- 지역: 대한민국, 초기 주요 지역은 서울·경기
- 특징: 빈티지 패션과 제품에 관심 있는 사용자 및 빈티지 숍 운영자
- 사용자는 근처의 좋은 빈티지 숍을 지도에서 찾고 상세 정보를 확인할 수 있다.
- 숍 운영자는 자신의 매장에 대한 관심도를 확인할 수 있다.
- 사용자는 빈티지 제품과 관련된 게시글과 댓글로 다른 사용자와 소통할 수 있다.

### 현재 제품 범위

- [x] 이메일·비밀번호·닉네임 회원가입
- [x] 로그인과 access/refresh 토큰 저장
- [x] 네이버 지도에서 빈티지 숍 마커와 목록 조회
- [x] 마커 선택 후 빈티지 숍 상세 정보 조회
- [x] 빈티지 숍 좋아요 등록·취소
- [x] 빈티지 숍 댓글·대댓글 조회와 작성
- [x] 커뮤니티 게시글 CRUD·좋아요·댓글
- [x] 게시글·게시판 댓글·매장 댓글 신고 접수와 내 신고 내역 조회
- [x] 사용자 차단·해제와 차단 목록 조회
- [x] 빈티지 YouTube 추천 목록 조회
- [x] 마이페이지의 닉네임·비밀번호 변경, 로그아웃, 회원 탈퇴
- [ ] 알림과 채팅

기존에 구현된 기능을 퇴행시키지 않는다. 제품 범위에 기능을 추가하거나 제거할 때 이 목록과 관련 화면·데이터 요구사항을 함께 갱신한다.

### 핵심 기능

- 회원가입
  - 이메일·비밀번호·닉네임으로 가입한다.
  - 만 14세 이상이며 이용약관과 개인정보처리방침에 동의한 경우에만 가입할 수 있다.
  - API: `POST /api/v1/members/join`
- 로그인
  - 가입한 정보로 로그인하고 access/refresh 토큰을 안전하게 저장한 뒤 메인 화면으로 이동한다.
- 빈티지 숍 탐색
  - `GET /api/v1/vintages`로 목록을 받아 네이버 Dynamic Map에 마커로 표시한다.
  - 마커를 선택하면 `GET /api/v1/vintages/{id}`로 이미지·이름·주소·좋아요·댓글 정보를 조회해 바텀시트에 표시한다.
- 빈티지 숍 좋아요
  - `POST/DELETE /api/v1/vintages/{id}/likes`로 좋아요를 등록하거나 취소한다.
- 빈티지 숍 댓글·대댓글
  - `POST /api/v1/vintages/{id}/comments`로 작성한다.
  - 일반 댓글은 `parentCommentId=0`, 대댓글은 부모 댓글 ID를 사용한다.
- 커뮤니티
  - 게시글 목록·상세·작성·수정·삭제, 좋아요와 댓글 기능을 제공한다.
- 신고
  - `POST /api/v1/reports`로 게시글·게시판 댓글·매장 댓글 신고를 접수한다.
  - 본인 콘텐츠에는 신고 기능을 노출하지 않고, 중복 신고 등 실패는 응답 본문의 `success`와 `code`로 처리한다.
  - `GET /api/v1/reports/me`로 로그인한 회원의 신고 내역을 조회한다.
  - 현재 관리자 검토 기능이 없어 신규 신고 상태는 `PENDING`이며 신고 즉시 콘텐츠를 숨기지 않는다.
- 사용자 차단
  - `POST /api/v1/blocks`, `DELETE /api/v1/blocks/{memberId}`, `GET /api/v1/blocks`로 차단·해제·목록 조회를 제공한다.
  - 차단은 단방향이며 차단한 회원의 게시글과 댓글은 현재 회원의 화면에서만 숨겨진다.
  - 게시글 상세는 서버 차단 필터가 적용되지 않으므로 작성자 차단 후 목록으로 돌아간다.
  - 댓글 작성 응답의 업무 오류 `code=403`은 인증 만료로 처리하지 않고 서버의 중립적인 실패 문구를 표시한다.
- 마이페이지
  - 사용자 정보 확인, 닉네임·비밀번호 변경, 로그아웃과 회원 탈퇴 기능을 제공한다.

### 입력 검증

- 닉네임
  - 2~10자
  - 정규식 `^[가-힣A-Za-z0-9_-]+$`
  - 한글·영문·숫자·`_`·`-`만 허용하고 공백은 허용하지 않는다.
- 이메일
  - 최대 64자
  - 공백을 허용하지 않는다.
  - `@`와 `.`을 포함하는 기본 이메일 형식을 확인한다.
- 비밀번호
  - 8~20자
  - 영문·숫자·특수문자를 각각 최소 1개 포함한다.
  - 공백을 허용하지 않는다.

### 화면과 사용자 흐름

- Root(`/`)
  - 저장된 토큰과 인증 상태를 확인해 로그인 또는 메인 화면으로 이동한다.
  - 확인 중에는 로딩 상태와 `VINTLY` 텍스트를 표시한다.
- 로그인·회원가입
  - 이메일·비밀번호·닉네임을 입력하고 필수 약관에 동의한 뒤 인증 완료 후 메인 화면으로 이동한다.
- 메인 화면
  - 빈티지 샵, 커뮤니티, YouTube, 마이 탭을 제공한다.
- 빈티지 샵
  - 네이버 지도, 숍 마커와 상세 바텀시트를 제공한다.
  - 상세 바텀시트에는 이미지·이름·주소·좋아요·댓글/대댓글과 입력 영역을 표시한다.
- 커뮤니티
  - 게시글 목록·상세·글쓰기 화면을 제공한다.
- YouTube
  - 빈티지 관련 YouTube 추천 목록을 제공한다.
  - `GET /api/v1/youtube-links`로 최신 등록순 추천 목록을 무한 스크롤 방식으로 페이지네이션 조회한다.
  - 추천 카드를 선택하면 서버가 제공한 YouTube URL을 외부 앱 또는 브라우저에서 연다.
- 마이
  - 계정 정보와 계정 관리 기능을 제공한다.

### 주요 데이터 모델

- `VintageShop`: `vintageId`, `name`, `state`, `district`, `detailAddr`, `lat`, `lon`, `thumbnailPath`
- `VintageShopDetail`: `vintageId`, `name`, `state`, `district`, `detailAddr`, `imgList`, `likeCount`, `liked`, `comments` 등
- `VintageComment`: `commentId`, `memberId`, `nickname`, `content`, `createdAt`, `parentCommentId`
- `VintageImage`: `vintageImgId`, `imgPath`
- `YoutubeLink`: `youtubeLinkId`, `url`, `title`, `description`, `isAd`, `createdAt`, `updatedAt`
- API 응답 모델은 실제 서버 계약을 기준으로 하며 서버에 없는 필드를 임의로 만들지 않는다.

### 디자인과 비기능 요구사항

- 브랜드 기본 색상은 브라운 `0xFF4E342E`와 그린 `0xFF2E7D32`이며 Material 3 스타일을 유지한다.
- 세부 브랜드 색상, 폰트와 로고 기준은 `docs/decisions`의 최신 결정을 따른다.
- 다크 모드는 현재 범위가 아니다.
- 첫 화면·목록 로딩과 지도 이동 시 눈에 띄는 끊김이 없도록 한다.
- 로딩, 빈 결과, 네트워크/API 오류 상태를 한국어로 명확히 전달한다.
- Android와 iOS를 지원하며 최소 지원 버전은 프로젝트의 실제 플랫폼 설정을 기준으로 한다.

## 기술 구조

- Flutter 3.x / Dart `^3.11.0` 프로젝트다.
- 기능 코드는 `lib/features/<feature>` 아래에서 `data`와 `presentation` 역할을 구분한다.
- 앱 진입점, 라우트, 메인 탭은 `lib/app`; API·인증·공통 UI는 `lib/shared`; 환경 설정은 `lib/config`에 둔다.
- 상태 관리는 현재의 `StatefulWidget`/`setState` 방식을 우선한다. 명시적인 요구나 충분한 근거 없이 상태관리 패키지 또는 별도 아키텍처를 도입하지 않는다.
- 라우팅은 `Navigator`와 `AppRoutes`의 named route 패턴을 유지한다.
- API 응답 모델은 서버 필드와의 매핑을 명시적으로 유지하고, 누락되거나 nullable인 필드에 안전하게 대응한다.

## API와 인증

- 인증이 필요한 요청은 `lib/shared/api/authenticated_api.dart`의 래퍼를 사용한다. 기능 화면에서 토큰 재발급 로직을 중복 구현하지 않는다.
- 401 응답 시 refresh 토큰으로 `/api/v1/auth/reissue`를 호출하고 원래 요청은 한 번만 재시도한다. 재발급 실패 또는 재시도 후 401이면 인증 만료 흐름을 유지한다.
- access/refresh 토큰은 `TokenStorage`와 `flutter_secure_storage`를 통해서만 저장한다. 토큰, 비밀번호, 쿠키 등 민감정보를 코드·문서·로그·테스트 fixture에 넣지 않는다.
- 요청 로그를 추가하거나 변경할 때 인증 헤더와 비밀번호 계열 필드를 반드시 마스킹한다.
- 환경별 진입점은 `lib/main_local.dart`, `lib/main_dev.dart`, `lib/main_prd.dart`다. `lib/main.dart`는 기본 개발 환경을 사용한다.
- `lib/config/backend_local.dart`, `backend_dev.dart`, `backend_prd.dart`는 로컬 환경 파일이며 Git에서 제외된다. 실제 URL이나 비밀값을 새로 커밋하지 않는다.

## 구현 규칙

- 변경 범위를 작게 유지하고 기존 feature 기반 구조와 명명 방식을 따른다.
- 공통 네트워크, 인증, 다이얼로그 로직은 `lib/shared`의 기존 구현을 재사용한다.
- 회원가입 검증은 위 제품 요구사항의 규칙을 보존한다.
- 비동기 작업 뒤 UI를 갱신하거나 `BuildContext`를 사용할 때 위젯의 mounted 상태를 확인한다.
- 로딩, 빈 결과, 네트워크/API 오류 상태를 사용자에게 한국어로 명확히 보여 준다.
- 새 패키지는 기존 SDK나 의존성으로 해결하기 어려울 때만 추가하고 `pubspec.yaml`과 lockfile을 함께 일관되게 유지한다.
- 생성 파일과 플랫폼 빌드 산출물은 직접 수정하거나 커밋하지 않는다.

## 검증

변경한 Dart 파일은 포맷하고, 작업 범위에 맞게 아래 검증을 실행한다.

```bash
dart format lib test
flutter analyze
flutter test
```

- 동작 변경에는 가능한 한 unit/widget test를 추가하거나 갱신한다.
- 인증, API 재시도, JSON 파싱, 입력 검증 변경은 성공 경로와 실패/경계값을 모두 확인한다.
- 백엔드나 기기 의존성 때문에 검증하지 못한 항목은 완료 보고에 구체적으로 남긴다.

## 변경 전 확인

- 작업 시작 시 `git status --short`로 사용자 변경사항을 확인하고 관련 없는 수정은 보존한다.
- 요구사항이 이 문서의 제품 요구사항과 충돌하거나 서버 계약이 불명확하면 임의로 API 계약을 만들지 말고 해당 차이를 알린다.
- 완료 시 변경 파일, 사용자에게 보이는 영향, 실행한 검증과 남은 제한사항을 요약한다.
